{
  "name": "parking-finder-de",
  "private": true,
  "version": "0.1.0",
  "description": "Repo-level tooling (commit linting, dev orchestration). App code lives in backend/, data-pipeline/, frontend/.",
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "concurrently": "^9.0.1",
    "husky": "^9.1.6",
    "wait-on": "^8.0.1"
  },
  "scripts": {
    "prepare": "husky",
    "dev": "concurrently -n db,backend,frontend -c blue,green,magenta \"docker compose up\" \"bash backend/start.sh\" \"npm --prefix frontend start\""
  }
}
